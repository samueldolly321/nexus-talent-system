node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json

# Fichiers uploadés (photos candidats)
/uploads/

# Dumps de base de données (contiennent des données + hashs — ne jamais versionner)
/nexus_dump.sql
*.dump

# Fichiers temporaires Office (verrous ~$)
~$*
